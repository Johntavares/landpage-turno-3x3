{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "ignoreCommand": "node -e 'process.exit(process.env.VERCEL_GIT_COMMIT_REF === \"gh-pages\" ? 0 : 1)'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
